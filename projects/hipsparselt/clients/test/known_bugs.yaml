# A list of known bugs/failures. The bug can be specific or general.
# A test case is considered as matching if all indicated parameters match.
# Wildcards can be used for the function
#example
#- { function: prune, a_type: bf16_r, b_type: bf16_r, c_type: bf16_r, d_type: bf16_r, compute_type: f32_r, transA: N, transB: N, M: 512, N: 512, K: 512, lda: 512, ldb: 512, ldc: 512, ldd: 512, alpha: 5.0, alphai: 0.0, beta: 0.0, betai: 0.0, known_bug_platforms: gfx908 }

Known bugs:
# ROCM-27091: hipSPARSELt spmm with FNUZ 8-bit inputs (f8_fnuz_r / bf8_fnuz_r) and
# f32 output intermittently fails the strict 4-ULP ASSERT_FLOAT_EQ check in
# clients/include/unit.hpp against the CPU reference (sub-ULP accumulation rounding).
# Gate on gfx942 (the only arch these FNUZ precisions are generated for) until the
# reference/tolerance fix lands. Remove these entries when ROCM-27091 is resolved.
- { function: "spmm*", a_type: f8_fnuz_r,  b_type: f8_fnuz_r,  known_bug_platforms: gfx942 }
- { function: "spmm*", a_type: bf8_fnuz_r, b_type: bf8_fnuz_r, known_bug_platforms: gfx942 }
